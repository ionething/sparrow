mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.github.ionething.sparrow.entity
  configuration:
    map-underscore-to-camel-case: true

mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.github.ionething.sparrow.entity
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      table-prefix: sp_